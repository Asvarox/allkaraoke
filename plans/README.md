# Implementation Plans

Generated by the improve skill on 2026-06-12. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| 001 | Regenerate a complete shared-song index | P1 | M | - | DONE |
| 002 | Preserve first-seen timestamps on shared-song upsert | P1 | S | 001 | TODO |
| 003 | Await admin queue mutations before navigating | P2 | S-M | 001 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale; finding fixed independently or approach
abandoned)

## Dependency notes

- 002 depends on 001 because both touch `upsertSharedSong` and the shared-song
  index write path. Land the index regeneration change first so the first-seen
  preservation can derive index entries from the final stored record.
- 003 depends on 001 because queue save/delete assertions read the admin index
  immediately after navigation; a complete, less race-prone index makes those
  assertions meaningful.

## Findings covered

- Finding 1: `regenerateIndex` only scans the first KV list page.
- Finding 2: repeated syncs overwrite `firstSeenAt`.
- Finding 3: queue save/delete navigates before mutation result is known.
- Finding 5: index updates are read-modify-write around one KV key. This is
  covered inside plan 001.

## Findings considered and rejected

- Finding 4: remote mic room entropy is low while default permission is write.
  The user selected findings 1, 2, 3, and 5 as relevant for planning, so this
  remains unplanned for now.
- Direction option: shared-song moderation status. Deferred because the selected
  work is about correctness of the current queue/index behavior.
- Direction option: admin UI design-system consolidation. Deferred because it is
  lower leverage than the selected shared-song data-integrity fixes.
